@model IEnumerable<paceproject.Models.Server>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
@if ((bool)ViewBag.IsAdmin)
{
<p>
    @Html.ActionLink("Create New", "Create")
</p>
}
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ServerName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Infrastructure.InfrastructureName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Department.DepartmentName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ServerName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Infrastructure.InfrastructureName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Department.DepartmentName)
        </td>
        @if ((bool)ViewBag.IsAdmin)
        {
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ServerID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ServerID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ServerID })
        </td>
        }else
        {
            <td>
           @Html.ActionLink("Details", "Details", new { id=item.ServerID })
           </td>
        }
    </tr>
}

</table>
