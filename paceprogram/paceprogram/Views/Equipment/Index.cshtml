@model IEnumerable<paceproject.Models.Equipment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@if ((bool)ViewBag.IsAdmin)
{
<p>
    @Html.ActionLink("Create New", "Create")
</p>
}
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EquipmentCategory)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EquipmentCategory)
        </td>
        @if ((bool)ViewBag.IsAdmin)
        {
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EquipmentID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EquipmentID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EquipmentID })
        </td>
        }else
        {
            <td>
           @Html.ActionLink("Details", "Details", new { id=item.EquipmentID })
           </td>
        }
    </tr>
}

</table>
